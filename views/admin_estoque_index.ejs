<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Estoque</title>
</head>
<body>
  <h1>Estoque</h1>
  <p><a href="/admin/estoque/novo-produto">+ Novo produto</a></p>
  <p><a href="/dashboard">← Voltar</a></p>

  <h2>Produtos</h2>

  <% if (products.length === 0) { %>
    <p>Nenhum produto cadastrado ainda.</p>
  <% } else { %>
    <ul>
      <% products.forEach(p => { %>
        <li>
          <a href="/admin/estoque/produto/<%= p.id %>"><b><%= p.name %></b></a>
          (<%= p.category || "sem categoria" %>)
        </li>
      <% }) %>
    </ul>
  <% } %>

<p><a href="/admin/estoque/historico">📜 Ver histórico de movimentações</a></p>


  <hr/>

  <h2>Variações</h2>

  <% if (variants.length === 0) { %>
    <p>Nenhuma variação cadastrada ainda.</p>
  <% } else { %>
    <table border="1" cellpadding="6">
      <tr>
        <th>Produto</th>
        <th>SKU</th>
        <th>Cor</th>
        <th>Tamanho</th>
        <th>Estoque</th>
      </tr>

      <% variants.forEach(v => { %>
        <tr>
          <td><%= v.product_name %></td>
          <td><%= v.sku || "-" %></td>
          <td><%= v.color || "-" %></td>
          <td><%= v.size || "-" %></td>
          <td><%= v.stock %></td>
        </tr>
      <% }) %>
    </table>
  <% } %>
</body>
</html>
